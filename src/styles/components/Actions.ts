import styled from "styled-components"
export const ButtonActions = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  transition: 0.3s;
  &:hover {
    transform: scale(1.2);
  }
`
